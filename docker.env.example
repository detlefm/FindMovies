# Beispiel-Umgebungsvariablen für docker-compose.
# Kopieren Sie diese Datei nach docker.env und passen Sie die Pfade an.

# HINWEIS: Verwenden Sie absolute Pfade.
# Ersetzen Sie die Beispielpfade durch die tatsächlichen Pfade auf Ihrem System.

# Beispiel für Windows:
# CONFIG_PATH=C:/docker-data/findmovies/config
# DATA_PATH=C:/docker-data/findmovies/data
# PLUGINS_PATH=C:/docker-data/findmovies/plugins

# Beispiel für macOS / Linux:
# CONFIG_PATH=/Users/ihr-benutzer/docker-data/findmovies/config
# DATA_PATH=/Users/ihr-benutzer/docker-data/findmovies/data
# PLUGINS_PATH=/Users/ihr-benutzer/docker-data/findmovies/plugins

# --- Bitte hier Ihre Pfade eintragen ---
CONFIG_PATH=C:/docker-data/findmovies/config
DATA_PATH=C:/docker-data/findmovies/data
PLUGINS_PATH=C:/docker-data/findmovies/plugins
